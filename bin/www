#!/usr/bin/env node
var debug = require('debug')('http11_http2');
var server = require('../app11');

var server = server.listen(process.env.PORT_HTTP11 || 3000, function() {
  debug('Express server listening on port ' + 3000);
});
